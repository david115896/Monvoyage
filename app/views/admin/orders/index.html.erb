
<h2> The orders </h2>

<table class="table">
  <thead>
    <tr>
      <th scope="col"></th>
      <th scope="col">Email user</th>
      <th scope="col">command date</th>
      <th scope="col">user profile</th>
      <th scope="col">items sold  </th>
    </tr>
  </thead>
  <tbody>
		<% i = 1	 %>
		<% for order in @orders  %>
			<tr>
				<th scope="row"><%= i%></th>
				<td><%= order.user.email %> </td>
				<td><%= order.create_at %></td>
				<td><%= link_to "Show profile user", user_path(user.id) %> </td>
				<td><%= link_to "Show details", user_path(user.id) %> </td>
			</tr>
			<% i +=1 %>
		<% end %>
  </tbody>
</table>
