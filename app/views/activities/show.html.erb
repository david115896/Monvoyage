<!-- <p id="notice"><%= notice %></p> -->
<!-- <%= @activity.name %> -->
<!-- <%= link_to 'Edit', edit_city_activity_path(params[:id], @activity) %> |
<%= link_to 'Back', city_activities_path %> -->
<%= render 'show_activities', activity: @activity%>

 <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 50%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>


    <div id="map"></div>
