<div class="full_page">
  <div class="container new_country">
      <div class="container">
        <div class="row">
          <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
            <div class="card card-signin my-5">
              <div class="card-body">
                <h2 class="page-section-heading text-center text-primary mb-0 font-weight-bold">
                  Create Activity
                </h2>

<%= form_with(model: @activity, local: true, url: new_city_activity_path) do |form| %>
  <% if activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity.errors.count, "error") %> prohibited this activity from being saved:</h2>

      <ul>
      <% activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group floating-label-form-group controls mb-0 pb-2">
    <%= form.label :name, class:"font-weight-bold" %>
    <%= form.text_field :name, class:'form-control' %>
  </div>

  <div class="form-group floating-label-form-group controls mb-0 pb-2">
    <%= form.label :description, class:"font-weight-bold" %>
    <%= form.text_area :description, class:'form-control' %>
  </div>

  <div class="form-group floating-label-form-group controls mb-0 pb-2">
    <%= form.label :price,class: 'font-weigth-bold'  %>
    <%= form.text_field :price, class: 'form-control' %>
  </div>

  <div class="form-group floating-label-form-group controls mb-0 pb-2">
  <%= form.label :ticket_url, class: 'font-weigth-bold' %>
  <%= form.text_field :ticket_url, class: 'form-control' %>
</div>
<div class="form-group floating-label-form-group controls mb-0 pb-2">
  <%= form.label :category, class: 'font-weigth-bold' %>
  <%= form.text_field :category, class: 'form-control' %>
</div>
<div class="form-group floating-label-form-group controls mb-0 pb-2">
  <%= form.label :duration, class: 'font-weigth-bold' %>
  <%= form.text_field :duration, class: 'form-control' %>
</div>
<div class="form-group floating-label-form-group controls mb-0 pb-2">
<%= form.submit "Create", class:"btn btn-lg btn-primary btn-block text-uppercase"%>
<% end %>
</div>
</div>
</div>
</div>
</div>
</div>
</div>
</div>

  
