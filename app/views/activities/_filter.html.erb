
	<div class= "row">
		<%= form_with scope: :city, url: city_path(city.id), local: true, method: :get  do |form| %>
			<% if activity.errors.any? %>
				<div id="error_explanation">
					<h2><%= pluralize(activity.errors.count, "error") %> prohibited this activity from being saved:</h2>

					<ul>
					<% activity.errors.full_messages.each do |message| %>
						<li><%= message %></li>
					<% end %>
					</ul>
				</div>
			<% end %>






			<div class="form-group floating-label-form-group controls mb-0 pb-2 col-2">
					<%= form.collection_select(:city_id, activities_categories.all, :id, :name) %>
			</div>

			<div class="form-group floating-label-form-group controls mb-0 pb-2 col-2">
					<%= form.collection_select(:city_id, cities.all, :id, :name) %>
			</div>
		<% end %>
	</div>
		

