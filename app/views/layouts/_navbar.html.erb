<nav class="navbar navbar-expand-lg navbar-default bg-default">
  <%= link_to static_index_path,class: 'navbar-brand' do %>Mytravel <%end%>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">


      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "Profile", user_path(current_user.id), class:"nav-link" %>
        <% end %>
      </li>


      <li class="nav-item">
				<% if (cookies[:tempo_organiser] != nil) && (cookies[:organiser_id] != nil) %>
          <%= link_to organiser_path(cookies[:organiser_id]), class: 'nav-link' do %>Cart <i class="fas fa-shopping-cart"></i><%end%>         
				<% else %>
					<%= link_to root_path, class: 'nav-link' do %>Cart <i class="fas fa-shopping-cart"></i><%end%>
				<% end %>
			</li>

      <li class="nav-item">
				<% if (cookies[:tempo_organiser] != nil) && (cookies[:organiser_id] != nil) %>
					<%= link_to 'Activities', city_activities_path(current_city_id), class:"nav-link" %>
				<% else %>
					<%= link_to 'Activities', root_path, class: 'nav-link' %>
				<% end %>
			</li>

      <li class="nav-item">
				


      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "New Travel", root_path, class:"nav-link" %>
        <% end %>
      </li>

      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "My organiser ", edit_organiser_path(cookies[:organiser_id]), class:"nav-link" %>
        <% end %>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
           aria-haspopup="true" aria-expanded="false">
          Login/out <i class="fas fa-sign-in-alt"></i>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if user_signed_in?%>
            <%= link_to('Logout',destroy_user_session_path, method: :delete,class: 'dropdown-item') %>
          <% else %>
            <%= link_to('Sign Up', new_user_registration_path,class:'dropdown-item') %>
            <%= link_to('Login', new_user_session_path,class: 'dropdown-item')%>
          <% end %>
        </div>
      </li>
    </ul>

  </div>
</nav>
